services:
  - type: web
    name: crashlab-v3
    env: docker
    dockerfilePath: ./Dockerfile
    autoDeploy: true
    envVars:
      - key: FLASK_ENV
        value: production
      - key: REACT_APP_API_URL
        value: ""
